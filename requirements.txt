# TNFS Daemon Python Dependencies
# No external dependencies required - uses only Python standard library
# Python 3.7+ required for dataclasses and pathlib features

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0 